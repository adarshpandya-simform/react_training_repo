import { createTheme } from "@mui/material";

// creating and exporting custom material ui theme
export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#E00082",
    },
    secondary: {
      main: "#172A3A",
    },
  },
});
