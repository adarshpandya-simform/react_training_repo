import React from "react";

// eslint-disable-next-line react/prop-types
const PresentationalComponent = ({ title }) => {
	return (
		<div>
			<span>{title}</span>
		</div>
	);
};

export default PresentationalComponent;
