import { Button } from "@chakra-ui/react";

/**
 * CustomizedComponent: to demonstrate
 * we can even add our own variant in
 * theme config
 */
const CustomizedComponent = () => {
  return <Button variant={"dank"}>customly styled button</Button>;
};

export default CustomizedComponent;
